# دليل المكونات والمكتبات البرمجية - مشروع Startawy

إليك دليل شامل ومُنسق باحترافية لكل مكتبة مستخدمة في مشروعك، مع شرح وظيفتها العامة، ودورها الدقيق في مشروعكم لتكون مستعداً تماماً لمناقشة المشروع وتوضيح كل التفاصيل التقنية أمام لجنة المناقشة بثقة كاملة.

---

## 1. إطار العمل والتشغيل الأساسي (Core Framework & Runtime)

### 🔹 `next`
* **ما هي؟**: إطار عمل مبني على React لتطوير تطبيقات الويب الكاملة (Full-stack).
* **دورها في المشروع**: هي العمود القبلي للمشروع بأكمله. تُسخدم لتوفير خوادم لتشغيل التطبيق، بناء الصفحات المحمية وعامة الوصول، توفير منافذ الـ API (مثل `/api/chat` و `/api/stripe`)، وتحسين سرعة تحميل الصور والمواقع عبر الـ Server-Side Rendering (SSR).

### 🔹 `react` & `react-dom`
* **ما هي؟**: مكتبة واجهات المستخدم الأشهر لتوليد وإدارة عناصر الواجهة التفاعلية.
* **دورها في المشروع**: تُستخدم لبناء جميع المكونات المرئية التفاعلية (Components)، واستخدام الـ Hooks مثل `useState` و `useEffect` لإدارة تفاعل المستخدم مع الواجهات.

### 🔹 `server-only`
* **ما هي؟**: مكتبة تضمن عدم تشغيل ملفات معينة إلا على خادم الويب (Server-Side) وتمنع استيرادها بالخطأ في واجهات المستخدم (Client-Side).
* **دورها في المشروع**: تُستدعى في ملف `lib/prisma.ts` لحماية كود الاتصال المباشر بقاعدة البيانات ومنع تسريبه لجهة العميل لحماية سرية البيانات والـ credentials.

---

## 2. قواعد البيانات والـ ORM (Database & ORM)

### 🔹 `@prisma/client` & `prisma`
* **ما هي؟**: Prisma هي أداة ORM (Object-Relational Mapping) حديثة تُسهّل التعامل مع قواعد البيانات دون الحاجة لكتابة استعلامات SQL معقدة يدوياً.
* **دورها في المشروع**: تُستخدم لتعريف الجداول والعلاقات في ملف `schema.prisma` (مثل علاقة المستخدم `User` مع الشركات الناشئة `StartupFounder` أو الدفعات `Payment`). وتُسهّل جلب وتعديل وحذف البيانات برمجياً عبر الـ Client في جميع الـ Actions والـ APIs.

### 🔹 `mysql2`
* **ما هي؟**: المحرك والتعريف البرمجي (Database Driver) الذي يسمح للغة Node.js بالاتصال والتخاطب مع قواعد بيانات MySQL.
* **دورها في المشروع**: تعمل في الخلفية وقت التشغيل (Runtime) لتمكين Prisma من تنفيذ الاستعلامات والاتصال بقاعدة بيانات MySQL الخاصة بالمشروع (المستضافة على Aiven Cloud أو محلياً).

### 🔹 `@prisma/adapter-mariadb`
* **ما هي؟**: موائم برمجي يسمح لـ Prisma بالاتصال المتوافق مع قواعد بيانات MariaDB/MySQL بكفاءة عالية.
* **دورها في المشروع**: تضمن استقرار عمليات الاستعلام وحماية الاتصال المتعدد بقاعدة البيانات السحابية والمحلية.

---

## 3. التحقق من الهوية والأمن (Authentication & Security)

### 🔹 `bcryptjs`
* **ما هي؟**: مكتبة لتشفير النصوص (Hashing) بذكاء وحماية أمنية عالية جداً.
* **دورها في المشروع**: تُستخدم لتشفير كلمات مرور المستخدمين قبل حفظها في قاعدة البيانات أثناء التسجيل (`RegisterForm`) ومقارنة كلمة المرور المشفرة عند تسجيل الدخول (`LoginForm`) أو عند تغيير كلمة المرور لمنع سرقتها حتى لو تم اختراق قاعدة البيانات.

### 🔹 `jose`
* **ما هي؟**: مكتبة متطورة وخفيفة الوزن لتوليد وتوقيع وقراءة رموز الأمان من نوع JWT (JSON Web Tokens).
* **دورها في المشروع**: تُستخدم لإصدار رموز أمان الجلسات (Sessions) وتخزينها في ملفات تعريف الارتباط (Cookies) والتحقق من هوية المستخدم وصلاحياته بشكل آمن ومشفر عند تنقله بين الصفحات المحمية (مثل صفحة لوحة التحكم والبروفايل).

### 🔹 `google-auth-library`
* **ما هي؟**: المكتبة الرسمية من Google للتحقق من هويات المستخدمين وصلاحيات الحسابات.
* **دورها في المشروع**: تُستخدم في كود تسجيل الدخول السريع باستخدام حساب Google عبر فك تشفير وتوثيق الرموز الأمنية التي تُرجعها خوادم Google للتأكد من هوية المستخدم وتسجيل دخوله بنجاح.

---

## 4. الذكاء الاصطناعي (AI & Generative AI)

### 🔹 `@google/generative-ai`
* **ما هي؟**: المكتبة البرمجية الرسمية للاتصال بنماذج الذكاء الاصطناعي المتطورة من Google (مثل **Gemini**).
* **دورها في المشروع**: تُسخدم لتشغيل المستشار المالي والتقني الذكي (AI Chatbot) في صفحة `ai-chatbot` حيث يتم إرسال استفسارات رواد الأعمال وبيانات مشاريعهم وتحليلها وتوليد إجابات وحلول فورية وذكية مخصصة لهم.

---

## 5. المدفوعات والاشتراكات (Payment Integration)

### 🔹 `stripe` & `@stripe/stripe-js`
* **ما هي؟**: Stripe هي بوابة المدفوعات الإلكترونية العالمية الأشهر.
* **دورها في المشروع**:
  * **`stripe` (على الخادم)**: تُستخدم لإنشاء جلسات الدفع الآمنة (Checkout Sessions) للترقية للخطط المدفوعة (Basic & PRO) ومعالجة إشعارات الدفع الفورية (Stripe Webhooks) لتفعيل الاشتراكات تلقائياً في قاعدة البيانات.
  * **`@stripe/stripe-js` (على العميل)**: تُستخدم في الواجهة الأمامية لعرض نماذج الدفع وتحميل مكتبة Stripe بأمان وسلاسة داخل متصفح المستخدم.

---

## 6. التحليل وتصدير الملفات (Data Visualization & File Export)

### 🔹 `recharts`
* **ما هي؟**: مكتبة مبنية على React لبناء رسوم بيانية تفاعلية ومتحركة وجذابة بصرياً.
* **دورها في المشروع**: تُستخدم في لوحة تحكم رائد الأعمال والمستشار والمسؤول لعرض تحليلات الميزانية، الإيرادات والنسب المالية برسوم بيانية توضيحية فاخرة تدعم الألوان والتأثيرات التفاعلية عند مرور الفأرة.

### 🔹 `jspdf` & `@types/jspdf`
* **ما هي؟**: مكتبة لتوليد وإنشاء ملفات PDF برمجياً مباشرة داخل تطبيق الويب.
* **دورها في المشروع**: تمكن رواد الأعمال من تحميل تقارير تحليل الميزانية والخطط الاستشارية كملفات PDF منسقة وجميلة بنقرة زر واحدة لحفظها محلياً ومشاركتها مع المستثمرين.

---

## 7. واجهة المستخدم والتفاعل (UI/UX & Styling)

### 🔹 `framer-motion`
* **ما هي؟**: أفضل مكتبة للأنيميشن والتأثيرات الحركية التفاعلية في تطبيقات React.
* **دورها في المشروع**: تمنح تطبيقكم رونقاً احترافياً عبر حركات الدخول السلسة للصفحات، التأثيرات التفاعلية عند فتح النوافذ المنبثقة (Modals)، وحركات التنبيهات المؤقتة (Toasts)، مما يوفر تجربة مستخدم (Premium UX) مدهشة.

### 🔹 `lucide-react`
* **ما هي؟**: حزمة تحتوي على آلاف الأيقونات المتجهية الحديثة وذات التصميم المتناسق والجميل.
* **دورها في المشروع**: تُستخدم لعرض جميع الأيقونات التوضيحية الأنيقة داخل القوائم الجانبية، لوحات التحكم، الأزرار، والبطاقات (مثل أيقونة المحفظة، المخطط البياني، الإعدادات، وقفل الأمان).

### 🔹 `next-themes`
* **ما هي؟**: مكتبة لإدارة المظهر البرمجي بسهولة (Dark Mode & Light Mode).
* **دورها في المشروع**: تسمح للمستخدم بالتبديل بين المظهر المظلم والداكن بسلاسة تامة، مع حفظ تفضيلاته داخل المتصفح بحيث لا تتغير عند إعادة تحميل الصفحة.

### 🔹 `clsx` & `tailwind-merge`
* **ما هي؟**: مكتبات لمعالجة وتجميع نصوص كلاسات CSS ودمجها بذكاء لمنع التداخل والتعارض.
* **دورها في المشروع**: تُستخدم لدمج كلاسات Tailwind CSS ديناميكياً داخل المكونات البرمجية لتغيير المظهر بناءً على حالة المكون (مثال: زر يتغير لونه تلقائياً إذا كان مفعلاً أو معطلاً).

---

## 8. النماذج والتحقق من المدخلات (Forms & Validations)

### 🔹 `formik`
* **ما هي؟**: مكتبة تسهل بناء النماذج (Forms) وإدارة قيم المدخلات وحالات الأخطاء وحالة الإرسال في React.
* **دورها في المشروع**: تُدير نماذج تسجيل الدخول، التسجيل، تعديل الملف الشخصي، وإدخال بيانات المشروع وتضمن عدم إرسال أي فورم إلا إذا تمت تعبئة الحقول بشكل سليم وتُقلل الأكواد المتكررة.

### 🔹 `yup`
* **ما هي؟**: مكتبة لوضع شروط صارمة وسهلة للتحقق من صحة البيانات (Validation Schema).
* **دورها في المشروع**: تضمن التحقق من صحة المدخلات بذكاء (مثال: أن يكون البريد الإلكتروني مكتوباً بصيغة صحيحة، كلمة المرور لا تقل عن 8 أحرف وتحتوي على رموز، وألا تُترك الحقول المطلوبة فارغة) وتعرض رسائل خطأ فورية وواضحة للمستخدم.

---

## 9. إدارة الحالة والاتصالات (State & Communications)

### 🔹 `zustand`
* **ما هي؟**: مكتبة حديثة وفائقة الخفة لإدارة الحالة العالمية للتطبيق (Global State Management).
* **دورها في المشروع**: تُسخدم لإدارة حالة التنبيهات الفورية (Toasts) بحيث تظهر وتختفي بسلاسة في أي مكان بالتطبيق دون تعقيد، وتُسهل تبادل البيانات العامة بين المكونات المتباعدة دون الحاجة لتمريرها عبر الـ Props.

### 🔹 `socket.io-client`
* **ما هي؟**: حزمة برمجية لربط واجهة المستخدم باتصالات الويب اللحظية ثنائية الاتجاه (WebSockets).
* **دورها في المشروع**: تُستخدم في نظام الدعم الفني وغرفة المحادثات الحية (الدردشة الفورية) لإرسال واستقبال الرسائل التفاعلية لحظة بلحظة وبدون الحاجة لعمل تحديث للصفحة.

### 🔹 `cloudinary`
* **ما هي؟**: مكتبة للربط البرمجي مع خوادم Cloudinary العالمية المخصصة لرفع وتخزين وتحسين وعرض الصور والملفات.
* **دورها في المشروع**: تُستخدم لرفع صور الحسابات الشخصية (Avatars) وشعارات الشركات الناشئة (Logos) وحفظها سحابياً بجودة ممتازة وسرعة تحميل فائقة وتوفير روابط آمنة لها.

### 🔹 `dotenv`
* **ما هي؟**: مكتبة لتحميل متغيرات البيئة السرية (Environment Variables) من ملفات الـ `.env`.
* **دورها في المشروع**: تحمي ملفات الاتصال السرية بداخل سكريبتات قواعد البيانات ومفاتيح الـ API (مثل مفتاح Stripe و Gemini) لضمان عدم حفظها بشكل مكشوف داخل الكود البرمجي.

---

## 10. أدوات التطوير (devDependencies)
*(هذه الحزم لا يتم رفعها للمستخدم النهائي، بل تُستخدم أثناء كتابة الكود فقط)*:
* **`typescript`**: لإضافة أمان كتابة المتغيرات والكشف المبكر عن الأخطاء البرمجية (Type Safety).
* **`tailwindcss` & `@tailwindcss/postcss`**: لبناء واجهات التطبيق الأنيقة والفاخرة بأسلوب التصميم السريع والمتجاوب.
* **`eslint` & `eslint-config-next`**: لفحص الكود ومراجعة التنسيق واكتشاف الأخطاء وتنبيهك لها تلقائياً لضمان كتابة كود نظيف ومحترف وخالٍ من الثغرات.
