# دليل التهيئة التفصيلي لمتغيرات البيئة وقاعدة البيانات - مشروع Startawy

مرحباً بك! هذا الدليل يشرح بالتفصيل الممل كيفية تهيئة ملف الـ `.env` للمطور الجديد، وكيفية إعداد قاعدة البيانات المحلية، واستخراج المنافذ (Local Ports) والمتغيرات المطلوبة لتشغيل المشروع محلياً 100%.

---

## 1. إعداد قاعدة البيانات واستخراج متغيراتها المحلية

يحتاج التطبيق للاتصال بقاعدة بيانات MySQL على جهازك المحلي. سنقوم بتهيئة قاعدة البيانات ثم ملء بياناتها في الملف.

### أ. إنشاء قاعدة البيانات المحلية
1. افتح برنامج إدارة قواعد البيانات الخاص بك (مثل **MySQL Workbench** أو **DBeaver** أو برنامج **phpMyAdmin** إذا كنت تستخدم حزمة XAMPP).
2. قم بالاتصال بخادم MySQL المحلي لديك (Localhost).
3. اكتب أمر SQL التالي لإنشاء قاعدة بيانات فارغة جديدة:
   ```sql
   CREATE DATABASE bis_db;
   ```

### ب. كتابة بيانات قاعدة البيانات في ملف `.env`
الآن، افتح ملف `.env` وقم بكتابة البيانات الفردية لقاعدة البيانات المحلية الخاصة بك:

```env
# 1. عنوان الخادم المحلي (في العادة يكون localhost أو 127.0.0.1)
DB_HOST="localhost"

# 2. المنفذ الافتراضي لـ MySQL (افتراضياً يكون 3306)
DB_PORT="3306"

# 3. اسم المستخدم الافتراضي لقاعدة البيانات (في الغالب يكون root)
DB_USER="root"

# 4. كلمة المرور الخاصة بقاعدة بياناتك (اتركها فارغة "" إذا لم تكن قد وضعت كلمة مرور عند التثبيت)
DB_PASSWORD="اكتب_كلمة_المرور_هنا_أو_اتركها_فارغة"

# 5. اسم قاعدة البيانات التي أنشأناها في الخطوة السابقة
DB_NAME="bis_db"
```

### ج. صياغة رابط الاتصال الموحد لـ Prisma (`DATABASE_URL`)
تقرأ مكتبة Prisma رابطاً موحداً يحتوي على كل البيانات السابقة مدمجة معاً.
قم بصياغة الرابط بناءً على بياناتك المحلية كالتالي:

* **إذا لم يكن لديك كلمة مرور لـ MySQL**:
  ```env
  DATABASE_URL="mysql://root@localhost:3306/bis_db"
  ```
* **إذا كانت لديك كلمة مرور (مثلاً: `123456`)**:
  ```env
  DATABASE_URL="mysql://root:123456@localhost:3306/bis_db"
  ```

---

## 2. تحديد المنفذ المحلي للتطبيق (Local Port & APP URL)

بشكل افتراضي، عند تشغيل المشروع عبر الأمر `npm run dev`، يقوم خادم Next.js بالعمل على المنفذ **`3000`**.

لذلك، يجب تهيئة المتغير التالي ليتوافق تماماً مع هذا المنفذ لكي تعمل روابط التحويل وإعادة التوجيه (مثلاً بعد الدفع بفيزا Stripe بنجاح):
```env
# المنفذ الافتراضي للـ Next.js محلياً
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```
*(ملاحظة: إذا قمت بتشغيل الخادم على منفذ آخر مثل 3001، يجب تعديل الرابط ليكون `http://localhost:3001`)*.

---

## 3. إعداد الروابط الخلفية للخدمات الخارجية (Backend & Socket)

يحتوي التطبيق على خدمات تفاعلية لحظية (مثل الدردشة الحية والـ WebSockets والاتصال بسيرفر خارجي):
```env
# رابط السيرفر الخلفي الخارجي (Backend API)
NEXT_PUBLIC_BACKEND_URL="https://api.devmostaf.me"

# رابط سيرفر الـ WebSockets لإدارة اتصالات غرف الدردشة اللحظية
NEXT_PUBLIC_SOCKET_URL="https://api.devmostaf.me"
```
*(ملاحظة: في بيئة التطوير المحلية للمطور الآخر، يمكنه إبقاؤها تشير للسيرفر التجريبي الخارجي `https://api.devmostaf.me` لكي تعمل الدردشة مباشرة، أو إذا كان لديه سيرفر محلي يعمل على منفذ آخر كـ Node.js، يقوم بتعديله لـ `http://localhost:3001`)*.

---

## 4. إعداد مفاتيح تسجيل الدخول السريع عبر Google

لتفعيل ميزة تسجيل الدخول بنقرة زر عبر حساب Google، يحتاج المطور لتهيئة حساب مطورين في Google Cloud Console:

1. الذهاب إلى [Google Cloud Console](https://console.cloud.google.com/).
2. إنشاء مشروع جديد وتفعيل **OAuth consent screen**.
3. الذهاب إلى تبويب **Credentials** ثم اختيار **Create Credentials** -> **OAuth client ID**.
4. اختيار نوع التطبيق: **Web application**.
5. إضافة الروابط المسموحة (Authorized redirect URIs):
   * إضافة: `http://localhost:3000/api/auth/callback/google` (أو الرابط المناسب لـ NextAuth).
6. سينتج لك قيمتان، قم بنسخهما ووضعهما في الملف كالتالي:
   ```env
   GOOGLE_CLIENT_ID="رقم_العميل_الخاص_بجوجل.apps.googleusercontent.com"
   NEXT_PUBLIC_GOOGLE_CLIENT_ID="نفس_رقم_العميل_السابق_هنا_أيضاً"
   GOOGLE_CLIENT_SECRET="المفتاح_السري_الخاص_بجوجل"
   ```

---

## 5. المتغيرات والرموز السرية الأخرى في ملف `.env`

قم بملء وتعبئة الرموز المتبقية كالتالي:

* **`JWT_SECRET`**: اكتب أي نص طويل وقوي من اختيارك لحماية تشفير الجلسات (مثال: `my_super_secret_session_token_key_123`).
* **`STARTAWY_SECRET_KEY`**: كود أمان داخلي للمشروع، اتركه كما هو افتراضياً: `STARTAWY_SECRET_123456`.
* **Gemini AI & Stripe & Cloudinary**: اتبع التعليمات الموضحة بالملف للحصول على المفاتيح الخاصة بك من حساباتك المجانية ووضعها بالملف.

---

## 6. الأوامر النهائية لتشغيل قاعدة البيانات محلياً

بعد الانتهاء من ملء ملف الـ `.env` بالكامل، نفذ الأوامر التالية بالترتيب في نافذة الأوامر بداخل مجلد المشروع:

```bash
# 1. تثبيت كل الحزم والمكتبات
npm install

# 2. بناء ونقل الجداول لقاعدة بياناتك المحلية (bis_db) المنشأة حديثاً
npx prisma db push

# 3. توليد أكواد وقوالب التعامل مع قاعدة البيانات
npx prisma generate

# 4. إدخال الباقات والخطط الأساسية لقاعدة البيانات المحلية
node prisma/seed_packages_standalone.js

# 5. تصفح جداول قاعدة بياناتك محلياً بشكل رسومي فاخر (Prisma Studio)
# سيفتح لك صفحة رسومية رائعة على الرابط http://localhost:5555 لتصفح الجداول
npx prisma studio

# 6. تشغيل السيرفر المحلي للتطبيق
npm run dev
```

الآن أصبح المشروع متصلاً بقاعدة بياناتك المحلية، ويعمل بالكامل على خادمك المحلي! 🎉
